import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../piyachon_wanburi_ux_ui_portfolio_MERGED';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
